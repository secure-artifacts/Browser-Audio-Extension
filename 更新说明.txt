1.2.1（回声处理修复）

- 根据用户提供的参考插件，确认其核心设置为 echoCancellation={exact:"remote-only"}。
- 修正原先强制 false 的行为；保留关闭自动增益和降噪、默认开启、全网站范围和原有图标。
- 初始采集、后续 applyConstraints、音轨克隆采用一致设置，不原地修改网页传入对象。
- 不支持远端回声抑制时显示错误，不悄悄退回无回声抑制。
- 面板改为显示远端回声抑制状态；false/true/all 均不能被误报为目标模式。
- 未复制参考插件源码或图标，未加入其控制台音频请求日志，无新增权限。
- 用户已反馈修复后实际通话测试正常；未记录具体同时通话数量，不宣称所有环境完全无回音。
- 安全预检需按当前版本重新核验，不沿用旧版结果；平台审批以管理员实际结果为准。

1.2.0（正式版 1.2）

- 插件名称统一为「浏览器拉线助手」，移除面板中的「测试版」标记。
- 同步更新安装说明和版本标识；音频处理功能、权限、图标与默认开关行为保持不变。
- 本次名称调整不代表商店上架或安全审核已经通过。

0.2.0

- 沿用桌面版 APP 的同款图标，直接提取其 ICO 内的 PNG 图像，未重新设计。
- 保持一个全局开关、首次安装默认开启，不增加「此网站除外」。
- 增加当前网页状态检测：等待采集、等待刷新、音频处理已关闭、静音、无法确认等。
- 麦克风权限拒绝、设备不可用、音频要求不兼容等失败显示中文提示。
- 诊断依据浏览器音轨原生设置，不伪造成功，不记录或上传音频。
- 整条麦克风音频流克隆、停止后重连、继承式设备约束等兼容处理。
- 正常关闭的音轨不再显示为仍在采集中，重试成功会清除旧错误。
- 保留关闭/开启状态，重启后继续生效；切换后仍需刷新通话网页。
- 新增测试覆盖；具体浏览器版本和未覆盖范围见「测试记录.txt」。

上架商店和自动更新：尚未实施。当前 ZIP 供本地安装或手动升级，不是已发布的商店版本。
